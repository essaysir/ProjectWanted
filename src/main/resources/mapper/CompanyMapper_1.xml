<?xml version="1.0" encoding="UTF-8"?>
<!-- ==== #29. mapper 기본설정 ==== -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.wanted.ProjectWanted.company.mapper.InterCompanyMapper_1">

	<resultMap type="HashMap" id="company_candidateList_Map">
		<result property="post_subject" 	column="post_subject" 	javaType="String" />
		<result property="name" 			column="name" 			javaType="String" />
		<result property="resume_subject" 	column="resume_subject" javaType="String" />
		<result property="applydate" 		column="applydate" 		javaType="String" />
		<result property="status" 			column="status" 		javaType="String" />
	</resultMap>
	<select id="getcandidateList" resultType="HashMap" resultMap="company_candidateList_Map">
		SELECT p.SUBJECT AS post_subject, m.NAME AS name, r.SUBJECT AS resume_subject, a.APPLYDATE
			 , CASE WHEN a.STATUS = '1' THEN '합격' 
			 		WHEN a.STATUS = '2' THEN '불합격' 
			 		WHEN a.STATUS = '3' THEN '채용전' END AS status
		FROM TBL_MEMBER M
		     JOIN TBL_RESUME r ON m.USERID = r.FK_USERID
		     JOIN TBL_APPLY a ON r.RESUME_CODE = a.FK_RESUME_CODE
		     JOIN TBL_POST p ON a.FK_POST_CODE = p.POST_CODE
		WHERE 1=1
		<if test="status != 0 and status != ''">AND STATUS = #{status}</if>
		ORDER BY a.APPLYDATE DESC
	</select>



	<!-- 총 지원서 건수(totalCount) 구하기 - 검색이 있을 때와 검색이 없을 때로 나뉜다. --> 
	<select id="getTotalCount" resultType="Integer">
		SELECT COUNT(*)
			<choose>
			<when test='searchType == ""'>
			FROM TBL_APPLY a
			</when>	
			<otherwise>
				<choose>
				<when test="searchType == 'subject'">
				FROM TBL_APPLY a JOIN TBL_POST p ON a.FK_POST_CODE = p.POST_CODE
								 
				WHERE LOWER(p.SUBJECT) LIKE '%'|| LOWER(#{searchWord}) || '%'		  
				</when>
				<otherwise>
				FROM TBL_APPLY a JOIN TBL_MEMBER m ON p.POST_CODE = a.FK_POST_CODE
				WHERE LOWER(a.NAME) LIKE '%'|| LOWER(#{searchWord}) || '%'
				</otherwise>
				</choose>
			</otherwise>	
			</choose>
		<if test="status != 0">
			WHERE a.STATUS = #{status}
		</if>
	</select>
	
	
	<!-- 페이징 처리한 지원서List 가져오기  --> 
	<select id="listSearchWithPaging" resultType="HashMap" resultMap="company_candidateList_Map">
		SELECT V.name AS name, V.resume_subject AS resume_subject, V.post_subject AS post_subject, V.applydate AS applydate,
		       CASE WHEN V.status = '1' THEN '합격'
		            WHEN V.status = '2' THEN '불합격' 
					WHEN V.STATUS = '3' THEN '채용전' END AS status
		FROM (
		    SELECT row_number() over(order by a.APPLYDATE desc) AS rno,
		           m.NAME AS name,
		           r.SUBJECT AS resume_subject,
		           p.SUBJECT AS post_subject,
		           a.APPLYDATE,
		           a.STATUS 
		    FROM TBL_MEMBER m
		         JOIN TBL_RESUME r ON m.USERID = r.FK_USERID
		         JOIN TBL_APPLY a ON r.RESUME_CODE = a.FK_RESUME_CODE
		         JOIN TBL_POST p ON a.FK_POST_CODE = p.POST_CODE
		    WHERE 1=1
			<if test='searchType != "" and searchWord != ""'>
				<choose>
				<when test="searchType == 'subject'">
				 AND lower(p.SUBJECT) LIKE '%' || lower(#{searchWord}) || '%'
				</when>
				<otherwise>
				AND lower(m.NAME) LIKE '%' || lower(#{searchWord}) || '%'
				</otherwise>
				</choose>
			</if>	
			<if test="status != 0">
				AND a.STATUS = #{status}
			</if>
		) V
		WHERE rno between #{startRno} and #{endRno}
		ORDER BY V.APPLYDATE DESC
	</select>
	

	
	<!-- 지원자 이력서 가져오기
	<select id="getApplyResume" resultType="String" resultMap="com.spring.wanted.ProjectWanted.member.model.ResumeVO">
		SELECT
		    r.SUBJECT AS resume_subject,
		    m.NAME AS name,
		    m.USERID AS email,
		    m.MOBILE AS mobile,
		    r.INTRODUCE AS introduce,
		    c.START_DATE AS career_start_date,
		    c.END_DATE AS career_end_date,
		    c.COMPANY AS company,
		    c.DEPARTMENT AS department,
		    c.CONTENT AS career_content,
		    s.START_DATE AS school_start_date,
		    s.END_DATE AS school_end_date,
		    s.SCHOOL AS shool,
		    s.MAJOR AS major,
		    s.CONTENT AS school_content,
		    r.SKILL AS skill,
		    rw.REWARD_DATE AS reward_date,
		    rw.REWARD AS reward,
		    rw.CONTENT AS reward_content,
		    l.LANG_DATE AS language_date,
		    l.FOR_LANG AS language,
		    l.CONTENT AS language_content,
		    r.UPLOADLINK AS link
		FROM
		    TBL_RESUME r
		JOIN
		    TBL_MEMBER m ON r.FK_USERID = m.USERID
		LEFT JOIN
		    TBL_CAREER c ON r.RESUME_CODE = c.FK_RESUME_CODE
		LEFT JOIN
		    TBL_SCHOOL s ON r.RESUME_CODE = s.FK_RESUME_CODE
		LEFT JOIN
		    TBL_REWARD rw ON r.RESUME_CODE = rw.FK_RESUME_CODE
		LEFT JOIN
		    TBL_LANGUAGE l ON r.RESUME_CODE = l.FK_RESUME_CODE
		where resume_code = #{resume_code}
	</select> --> 
	
	<!-- 지원자 이력서 가져오기 -->
	<select id="getApplyResume" parameterType="com.spring.wanted.ProjectWanted.member.model.ResumeVO">
		SELECT (RESUME_CODE, FK_USERID, ISCOMPLETE, SUBJECT, INTRODUCE, UPLOADLINK)
		FROM TBL_RESUME
	</select>
	
	<!-- 지원자 이력서 Language -->
	<select id="getLanguage" parameterType="com.spring.wanted.ProjectWanted.member.model.LanguageVO">
		SELECT LANGUAGE_CODE, FK_RESUME_CODE, FOR_LANG, CONTENT, LANG_DATE
		from tbl_language
	</select>
	
	<!-- 지원자 이력서 Reward -->
	<select id="getReward" parameterType="com.spring.wanted.ProjectWanted.member.model.RewardVO">
	    SELECT REWARD_CODE, FK_RESUME_CODE, REWARD, CONTENT, REWARD_DATE
	    from tbl_reward
	</select>
	
	<!-- 지원자 이력서 School -->
	<select id="getSchool" parameterType="com.spring.wanted.ProjectWanted.member.model.SchoolVO">
	    SELECT SCHOOL_CODE, FK_RESUME_CODE, SCHOOL, MAJOR, CONTENT, START_DATE, END_DATE
	    from tbl_school seq_school.nextval, #{fk_resume_code}, #{school}, #{major}, #{content}, #{start_date}, #{end_date}
	</select>
	
	<!-- 지원자 이력서 Career -->
	<select id="getCareer" parameterType="com.spring.wanted.ProjectWanted.member.model.CareerVO">
	    select CAREER_CODE, FK_RESUME_CODE, COMPANY, DEPARTMENT, START_DATE, END_DATE
	    from tbl_CAREER
	</select>

	<!-- 지원자 이력서 Performance -->
	<select id="getPerformance" parameterType="com.spring.wanted.ProjectWanted.member.model.PerformanceVO">
	    select PERFORM_CODE, FK_CAREER_CODE, FK_RESUME_CODE, START_DATE, END_DATE, PERFORMANCE, PERFROM_CONTENT
	    from tbl_performance
	</select>

	<!-- 지원자 이력서 MemberTech -->
	<select id="getMemberTech" parameterType="com.spring.wanted.ProjectWanted.member.model.MemberTechVO">
	    select MEMBER_TECH_CODE, FK_TECH_CODE, FK_USERID, FK_RESUME_CODE
	    from tbl_Member_Tech
	</select>
	
	
	




</mapper>